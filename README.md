# Calendar Event Adder

Add events to Google Calendar by describing them in natural language.

## Setup

1. `pip install -r requirements.txt`
2. `.env` with `OPENAI_API_KEY` or `OPENAI_KEY`, and optionally `CALENDAR_ID`
3. Service account JSON key in project root
4. Share your Google Calendar with the service account email
